
function Stats(props){
    return (
        <div>
            <p>{props.statsName}</p>
            <p>{props.statNumber}</p>
        </div>
        )
}

export default Stats;

//  {items.map((item, index) => (
